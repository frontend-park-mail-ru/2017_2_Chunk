server {
    listen 80;
    server_name guardians-game.ru;
    root /home/guardians/public;

    # Redirect non-https traffic to https
    location / {
        return 301 https://$server_name$request_uri;
    }

    location /.well-known/acme-challenge/ {
        root /home/guardians/webroot;
    }
}

server {
    listen 443 ssl http2;
    server_name guardians-game.ru;
    location /^(\w+)$ {
        rewrite ^(.*)$ / break;
    }

     # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/guardians-game.ru/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/guardians-game.ru/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

    location / {
        root /home/guardians/public;
    }
}
